export const environment = {
  production: true,
  // Production - Backend URL (update with your production backend URL)
  apiBaseUrl: 'http://localhost:8080' // Update this with your production backend URL
};
