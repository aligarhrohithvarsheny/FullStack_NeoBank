export const environment = {
  production: true,
  // Production backend (Render). Overridden at build time by replace-env.js when BACKEND_API_URL is set.
  apiBaseUrl: 'https://fullstack-neobank.onrender.com'
};
